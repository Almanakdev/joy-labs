Put static assets here — they're served at the site root (/).

Required:
  joylabs-logo.png   -> used as the navbar/footer logo and the browser favicon.

Save your Joy Labs logo image in THIS folder with the exact name:
  joylabs-logo.png

Then hard-refresh the site (Ctrl+Shift+R).
